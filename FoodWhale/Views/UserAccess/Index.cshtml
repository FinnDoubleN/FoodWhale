@model IEnumerable<FoodWhale.Model.UserAccess>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="u-align-left u-container-style u-grey-5 u-group u-group-2">
    <a style="height:fit-content; margin-top: -2rem; margin-left: 1rem" asp-action="Create">Add</a>
    <table class="table" style="margin-left:-2.8rem">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DateAccess)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UidNavigation)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DateAccess)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UidNavigation.Uid)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Aid">Edit</a><br />
                        <a asp-action="Delete" asp-route-id="@item.Aid">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>